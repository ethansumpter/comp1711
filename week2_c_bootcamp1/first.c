#include <stdio.h>

int main(){
    int a = 10; // Initialises a variable with a value. C requires giving the data type for variables.
    printf("%d\n", a); // The %d will format the value as an integer (number)
    return 0;
}